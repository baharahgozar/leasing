import { Resource } from "../generics/Resource";

export class CarAccessory extends Resource {
    accessoryCode: number;
    accessoryPrice: number;
    carRef: string;
    active: boolean;
    garnerRef: number;
    accessoryCount: number;

}

